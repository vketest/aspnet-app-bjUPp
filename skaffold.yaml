apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/vketest/aspnet-app-bjupp:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/vketest/aspnet-app-bjupp:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: aspnet-app-bjupp
        chartPath: charts/aspnet-app-bjupp
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/vketest/aspnet-app-bjupp"
          image.tag: "{{.DIGEST_HEX}}"
